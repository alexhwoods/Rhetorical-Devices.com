---
import { getImage } from "astro:assets";

import Shakespeare from "../images/Shakespeare.jpg";
import ComboBox from "../components/Combobox.svelte";

// these are actually kind of wrong. I think they're one off in name
const smImage = await getImage({
  src: Shakespeare,
  quality: "high",
  width: 50,
  height: 50,
});

const mdImage = await getImage({
  src: Shakespeare,
  quality: "high",
  width: 60,
  height: 60,
});

const lgImage = await getImage({
  src: Shakespeare,
  quality: "high",
  width: 90,
  height: 90,
});

const xlImage = await getImage({
  src: Shakespeare,
  quality: "high",
  width: 400,
  height: 400,
});
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro + TailwindCSS</title>
  </head>

  <body>
    <div class="container mx-auto p-4">
      <div
        class="grid grid-cols-1 xl:grid-cols-2 mt-9 xl:mt-2 sm:mt-16 lg:mt-32"
      >
        <div class="flex justify-center xl:block">
          <picture>
            <!-- TODO: add one more -->
            <source media="(max-width: 767px)" srcset={smImage.src} />
            <source media="(max-width: 1023px)" srcset={mdImage.src} />
            <source media="(max-width: 1279px)" srcset={lgImage.src} />
            <source media="(min-width: 1280px)" srcset={xlImage.src} />
            <img class="xl:mt-32 xl:ml-32 rounded" src={xlImage.src} alt="" />
          </picture>
        </div>
        <div
          class="p-4 flex flex-col items-center xl:items-start xl:pt-72 xl:order-first"
        >
          <h2
            class="text-3xl xl:text-5xl text-center xl:text-left font-serif py-0 mb-7 xl:mb-[44px] w-48 xl:w-96 xl:leading-normal"
          >
            Speak Like Shakespeare
          </h2>
          <div class="w-full sm:max-w-md md:max-w-lg xl:max-w-md">
            <ComboBox client:load />
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
